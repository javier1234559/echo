# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=   # any random string

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# AI — server-side only, never use NEXT_PUBLIC_ for API keys
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini   # cheap model for testing; swap to gpt-4o for production
